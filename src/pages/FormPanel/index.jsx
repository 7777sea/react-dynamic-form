import React, { useContext } from 'react';
import { StoreCtx } from '@/store';
 

const FormPanel = () => {

    console.log(useContext(StoreCtx))
    return <div>

    </div>
}

export default FormPanel;